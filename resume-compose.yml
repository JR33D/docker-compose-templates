version: '3.3'

services:
  resume:
    image: ghcr.io/jr33d/resume-jreed-me:latest
    deploy:
      replicas: 1
    ports:
      - 3000:3000
      - 3001:3001
    environment:
      SENDGRID_API_KEY:
      CONTACT_EMAIL:
      APP_EMAIL:
    restart: unless-stopped